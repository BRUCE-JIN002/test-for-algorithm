/**
 * Function对象和 Object对象之间的关系：
 * Function是顶层的构造器 Object是顶层的对象
 *
 * hasOwnProperty: obj.hasOwnProperty('属性名') //false
 * isPrototypeOf: 是用来判断指定对象obj1否存在于另一个对象obj2的原型链中，是则返回true,否则，返回false
 * getProtoTypeOf: 是ES5中用来得到obj对象的原型的标准方法。
 */

//应用： 继承

/**
 * 继承的方式：
 *
 *
 *
 *
 * 继承的应用：
 */