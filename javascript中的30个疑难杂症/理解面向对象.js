/**
 * 什么是对象:
 * 1. 具有私有属性
 * 2. 只要是 new出来的都是对象, new Fn() => 实例化
 * 3. 不同对象肯定不同 var a=[1], var b=[1] , a !== b
 * 4. 对象都会有引用机制， 堆 栈
 */

/**
 * JS中万物皆对象：Array Date Object Function String
 * 1. 字面量——字面上显示的东西
 * 2. 包装类： 没有 new 的函数声明，可以理解为不是对象，string、Number
 */

/**
 *面向对象： (初级和中级工程师的分水岭)
 把任何的数据和行为抽象成一个形象的对象， 类似于人生活中思考的方式。
 类： 对象(Object)的模板, 定义了同一组的对象 (又称实例)共有的属性和方法。
 
 面向对象oop：
 继承: 子类继承父类
 封装: 方法，包装： function(){}
 多态: 重载、重写: 覆盖继承自父类原有的方法

 */